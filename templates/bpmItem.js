if (Meteor.isClient) {
	
	Template.bpmItem.helpers({
		
	});
	
	Template.bpmItem.events({
/*
	  'click .toggle-checked': function() {
		  Meteor.call("updateBpmItem", this._id, !this.checked);
	  },
	  'click .delete': function() {
		  Meteor.call("deleteBpmItem", this._id);
	  },
	  'click .toggle-private': function() {
		  Meteor.call("setPrivate", this._id, !this.private);
	  }
*/
	});
}